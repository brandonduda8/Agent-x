from core.genesis.mission_controller import mission_controller


print(
    mission_controller.execute(
        "Find the fastest path to first revenue event"
    )
)


print(
    mission_controller.report()
)
