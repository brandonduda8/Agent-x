# Agent X — Python Requirements
# ================================
# Install with: pip install -r requirements.txt
#
# Termux note: heavyweight ML packages (transformers, torch) are optional
# and may fail on ARM without pre-built wheels.  Install separately if needed:
#   pip install transformers
# All packages below install cleanly on Termux without native compilation.

# ── Web / API ─────────────────────────────────────────────────────────────────
flask>=2.3.0
flask-socketio>=5.3.0
flask-cors>=4.0.0
eventlet>=0.33.0          # async driver for Flask-SocketIO (no gevent needed)

# ── HTTP ──────────────────────────────────────────────────────────────────────
requests>=2.31.0

# ── Configuration ─────────────────────────────────────────────────────────────
pyyaml>=6.0
python-dotenv>=1.0.0

# ── Payments ──────────────────────────────────────────────────────────────────
stripe>=5.0.0

# ── Scheduling / process management ──────────────────────────────────────────
schedule>=1.2.0
psutil>=5.9.0             # used by process-manager.py for PID checks

# ── CLI / display ─────────────────────────────────────────────────────────────
colorama>=0.4.6
rich>=13.0.0

# ── Data / utilities ──────────────────────────────────────────────────────────
python-dateutil>=2.8.2

# ── Optional: ML / NLP (comment out if Termux build fails) ───────────────────
# transformers>=4.30.0
# torch                    # very large; use CPU-only wheel on ARM if needed
# sentencepiece            # tokenizer for some HuggingFace models
